# S2: Plaid API Client and Sync

Covers: Plaid client configuration from credentials/env, sandbox public token
creation, public-to-access token exchange, account metadata fetch, institution
info fetch, transaction sync with cursor-based pagination. Environment switching
(sandbox/development/production).

Requirements traced: §1 (all bullets), §2 (all bullets).
Dependencies: S1 (stores Items, accounts, transactions, cursors in DB).
